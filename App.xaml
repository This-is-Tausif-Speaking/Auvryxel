<Application x:Class="Auvryxel.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
  <Application.Resources>
    <SolidColorBrush x:Key="AppBackground" Color="#F7F8F5" />
    <SolidColorBrush x:Key="ChromeBackground" Color="#ECEFEA" />
    <SolidColorBrush x:Key="SurfaceBackground" Color="#FCFDFB" />
    <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF" />
    <SolidColorBrush x:Key="FieldBackground" Color="#F0F2EE" />
    <SolidColorBrush x:Key="MainText" Color="#18211C" />
    <SolidColorBrush x:Key="SecondaryText" Color="#3E4B43" />
    <SolidColorBrush x:Key="MutedText" Color="#818B83" />
    <SolidColorBrush x:Key="BorderBrushColor" Color="#E5E9E3" />
    <SolidColorBrush x:Key="AccentBrush" Color="#267A64" />
    <SolidColorBrush x:Key="AccentSoft" Color="#E6F0EA" />
    <SolidColorBrush x:Key="ButtonText" Color="#59635D" />
    <SolidColorBrush x:Key="HoverBackground" Color="#E4E9E3" />
    <SolidColorBrush x:Key="PressedBackground" Color="#DCE3DC" />
    <SolidColorBrush x:Key="LogoMark" Color="#D7FFF1" />

    <Style TargetType="Button" x:Key="IconButton">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="{DynamicResource ButtonText}" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="10,7" />
      <Setter Property="FontSize" Value="15" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bd" Property="Background" Value="{DynamicResource HoverBackground}" /></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="bd" Property="Background" Value="{DynamicResource PressedBackground}" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Application.Resources>
</Application>
